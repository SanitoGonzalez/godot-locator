[project]
name = "godot-locator-tests"
version = "0"
description = "Integration tests for godot-locator"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "websockets>=13",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["."]
