[project]
name = "godot-locator-docs"
version = "0"
description = "Documentation site for godot-locator, built with MkDocs Material."
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.7.6",
]

[tool.uv]
package = false
